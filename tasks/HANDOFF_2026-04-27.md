# OHI Tech Website — HANDOFF 2026-04-27

## 현재 상태 요약

이번 세션에서 완료한 작업:
1. **Insights 3단계 네비게이션 구현** — `/insights` → `/insights/[category]` → `/insights/[category]/[slug]`
2. **SEO/GEO 전면 구성** — JSON-LD (Article, FAQPage, BreadcrumbList, Organization), og:image, canonical, hreflang, RSS autodiscovery
3. **postbuild 자동화** — `npm run build` 후 static/public 자동 복사
4. **308 리디렉션 우회** — `[category]/[slug]/page.tsx` 생성으로 목적지 URL 유효화
5. **Next.js 파라미터 충돌 해결** — `[slug]/` → `[category]/` 디렉토리 이름 변경

마지막 커밋: `02e0a90 fix(insights): rename [slug] to [category] — fix Next.js param name conflict`

현재 서버 상태: **정상 동작** — 모든 URL 200 OK

---

## 🚨 다음 세션에서 할 일 (IMMEDIATE) — 옵션 B 진행

### 핵심 미해결 문제: Linux Next.js 308 리디렉션 근본 원인 규명

**현상**: 같은 코드, 같은 Next.js 16.1.6인데
- macOS (로컬): 308 리디렉션 0개
- Linux (서버): 5개 308 리디렉션 자동 생성 (`/insights/esc` → `/insights/semiconductor-parts/esc` 등)

현재는 목적지 URL에 라우트를 만들어 우회했지만, 근본 원인을 모름.
**이 미스터리를 해결하고 URL 구조를 하나로 통일해야 함.**

---

### 조사 계획 (Opus가 설계한 계획)

**Step 1 — 서버 현황 스냅샷** (5분)
```bash
# 서버에서 실행
ssh os.ohitech.co.kr

# 308 리디렉션 목록 확인
cat ~/ohitech/ohitech-website/.next/routes-manifest.json | python3 -m json.tool | grep -A 10 '"redirects"'

# 소스 트리 잔존 파일 확인
ls -la ~/ohitech/ohitech-website/src/app/insights/

# Node, npm 버전 확인
node --version && npm --version

# package-lock.json 해시 비교 (로컬 vs 서버)
md5sum ~/ohitech/ohitech-website/package-lock.json
```

로컬에서도:
```bash
cat .next/routes-manifest.json | python3 -m json.tool | grep -A 10 '"redirects"'
md5sum package-lock.json
node --version && npm --version
```

**Step 2 — 서버 클린 빌드 테스트** (10분)
```bash
ssh os.ohitech.co.kr "cd ~/ohitech/ohitech-website && rm -rf .next node_modules && npm ci && npm run build"
# 빌드 후 확인
ssh os.ohitech.co.kr "cat ~/ohitech/ohitech-website/.next/routes-manifest.json | python3 -m json.tool | grep -A 10 'redirects'"
```

**Step 3 — 결과 분기**

- **케이스 A: 308 사라짐** → H1 확정 (캐시 문제). URL 구조 결정 후 깔끔하게 정리
- **케이스 B: 308 여전히 발생** → 다시 opus로 전환하여 깊은 디버깅

  케이스 B 추가 조사:
  ```bash
  # Next.js 빌드 trace 활성화
  NEXT_DEBUG_BUILD=1 npm run build 2>&1 | grep -i redirect
  
  # 빌드 산출물 비교 (서버 vs 로컬)
  # routes-manifest.json diff
  ```

**Step 4 — 근본 원인 확정 후 URL 전략 결정**

| | Flat URL (`/insights/esc`) | 2-level URL (`/insights/semiconductor-parts/esc`) |
|---|---|---|
| SEO | 짧고 공유 친화적, link equity 손실 없음 | 카테고리 컨텍스트 명확 |
| 현재 상태 | 308 발생 (Linux) | canonical로 동작 |

**Step 5 — 클린 구현** (결정 후)

URL 구조 통일 후:
- 다른 구조는 명시적 영구 리디렉션 (자동 생성 의존 X)
- `[category]/page.tsx`의 dual-purpose 분리 (카테고리 listing + flat 아티클 fallback이 섞여 있음)
- **사이트맵 수정** (sitemap.ts):
  - 아티클 URL을 canonical URL로 변경
  - 인사이트 카테고리 페이지 4개 추가
  - 한국어 alternate URL `?lang=ko` 제거
- 내부 링크, JSON-LD URL 모두 일관되게 정리

---

## 현재 URL 구조 (우회 상태)

```
/insights                                → 카테고리 카드 4개
/insights/[category]                     → 카테고리 목록 OR flat 아티클 fallback
/insights/[category]/[slug]              → 아티클 상세 (canonical)

서버가 자동 생성하는 308:
/insights/esc               → /insights/semiconductor-parts/esc
/insights/wafer-carrier     → /insights/semiconductor-parts/wafer-carrier
/insights/dry-vacuum-pump   → /insights/semiconductor-parts/dry-vacuum-pump
/insights/oring             → /insights/semiconductor-parts/oring
/insights/waterjet-laser    → /insights/laser-equipment/waterjet-laser
```

---

## 핵심 파일 위치

- 아티클 데이터: `src/app/insights/_data.ts`
- 카테고리/아티클 페이지: `src/app/insights/[category]/page.tsx`
- 아티클 상세 (2-level): `src/app/insights/[category]/[slug]/page.tsx`
- 인사이트 목록: `src/app/insights/page.tsx`
- SEO 메타데이터: `src/app/insights/_seo.tsx`
- 사이트맵: `src/app/sitemap.ts` ← 아직 flat URL 사용 중 (수정 필요)
- robots.txt: `src/app/robots.ts`
- 콘텐츠 가이드: `.claude/insights-content-guide.md`

## 서버 접속 정보

```bash
ssh os.ohitech.co.kr
cd ~/ohitech/ohitech-website

# 배포 (항상 이렇게)
git checkout src/
git pull origin main
npm run build       # postbuild 훅이 standalone 폴더에 static + public 자동 복사
pm2 restart ohitech
```

## 다음 대화 시작 프롬프트

```
/Users/jino/ohitech-website/tasks/HANDOFF_2026-04-27.md 읽고 이어서 작업해줘
```
